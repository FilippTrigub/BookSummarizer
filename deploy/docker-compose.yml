version: '3.8'

services:
  app:
    image: trigubtechnologiesprojectsacr.azurecr.io/audiosummarizer-api
    build:
      context: ../app
      dockerfile: Dockerfile
    restart: always
    ports:
      - 8081:8081
    networks:
      - audio-summarizer-network

#  ui:
#    image: trigubtechnologiesprojectsacr.azurecr.io/audiosummarizer-ui
#    build:
#      context: ../UI
#      dockerfile: Dockerfile
#      args:
#        - API_URI=http://localhost:8081
#    restart: always
#    ports:
#      - 80:80
#    depends_on:
#      - app
#    networks:
#      - audio-summarizer-network


networks:
  audio-summarizer-network: