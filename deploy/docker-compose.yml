version: '3.8'

services:
  api:
    image: generalregistry.azurecr.io/booksummarizer_api
    build:
      context: ../api
      dockerfile: Dockerfile
    restart: always

  ui:
    image: generalregistry.azurecr.io/booksummarizer_ui
    build:
      context: ../UI
      dockerfile: Dockerfile
    restart: always
